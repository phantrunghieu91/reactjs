import {Component} from 'react';

class ToBeDelete extends Component {
    render(){
        return (
            <div style={{
                backgroundColor: 'yellow'
            }}>
                This component will be delete if you click the button below.
            </div>
        );
    }
};

export default ToBeDelete;